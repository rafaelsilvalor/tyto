dist/
node_modules/
.turbo/
coverage/
pnpm-lock.yaml
**/__fixtures__/**/*.html
**/__fixtures__/**/*.svg

# Exporter output, compared byte for byte. Reflowing it would fail the snapshot it is.
**/__snapshots__/**/*.html
**/__snapshots__/**/*.svg
docs/patterns-explained.pt-BR.html

# Generated by `pnpm docs:gen`; formatting them here would fight the generator.
docs/diagnostic-codes.md
docs/render-contract.md
docs/render-result.schema.json

# Upstream licence text, redistributed verbatim (fonts/README.md). Reflowing a licence is
# not a formatting decision this repository gets to make.
fonts/*/LICENSE.md

# Tyto template markup. It looks like HTML+CSS and is not: Prettier reflows it as a
# stylesheet and rewrites `"Source Sans 3"` to single quotes, which the grammar refuses
# (docs/template-authoring.md says so about its own code block).
packages/templates/templates/**/*.html
