/**
 * @tyto/desktop — the Electron application.
 *
 * The electron-vite scaffold, typed preload and IPC contracts arrive with E9.1; this is a
 * placeholder so the workspace and the root `dev` script have a target.
 */
export {};
