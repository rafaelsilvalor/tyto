/**
 * @tyto/core — the vocabulary every other package speaks.
 *
 * Holds the Scene IR (Zod schemas and inferred types), `Result`/`Diagnostic`, the
 * `SceneVisitor` contract, the ports adapters implement, the template SDK, and the
 * resolve and compile stages. Pure: no Node, no DOM (ADR 0010).
 *
 * Filled in by TYTO-13 (E1.2) onwards.
 */
export {};
