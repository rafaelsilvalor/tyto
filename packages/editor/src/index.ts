/**
 * @tyto/editor — CodeMirror 6 for the brief and template languages.
 *
 * DOM only: no Node imports, because the Electron renderer runs with
 * nodeIntegration: false.
 *
 * Filled in by epic E8.
 */
export {};
