/**
 * @tyto/io — `BriefSource` and `OutputSink` ports with filesystem adapters.
 *
 * The inbox/outbox contract that Jacurutu talks to (ADR 0011) lands here.
 *
 * Filled in by epic E6.
 */
export {};
