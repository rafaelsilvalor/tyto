/**
 * @tyto/plugin-api — extension points, plugin manifest and host contract.
 *
 * Types only, so it stays pure: the host implementation lives in a Node package.
 *
 * Filled in by epic E7.
 */
export {};
