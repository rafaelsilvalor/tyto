/**
 * @tyto/template-lang — parser for the HTML-like template markup.
 *
 * Compiles the markup into the same template function shape the TypeScript path
 * produces, so both converge on `Scene`.
 *
 * Filled in by TYTO card E4.3.
 */
export {};
