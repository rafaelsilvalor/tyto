{
  "name": "@tyto/contract-test",
  "private": true,
  "type": "module",
  "description": "Runs the tyto binary against a fixture task folder and validates the ADR 0011 contract.",
  "license": "UNLICENSED",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@tyto/brief-lang": "workspace:*",
    "@tyto/cli": "workspace:*",
    "@tyto/core": "workspace:*",
    "@tyto/io": "workspace:*",
    "@tyto/pipeline": "workspace:*"
  },
  "devDependencies": {
    "@tyto/fonts": "workspace:*",
    "ajv": "^8.17.1",
    "vitest": "^5.0.1"
  }
}
